import {useState} from 'react';
import {ArrowUpRight,Users,ShoppingBag,Wallet,Clock3,Activity,ArrowRight} from 'lucide-react';
import {useResource} from '../hooks/useResource';
import type {Overview as OverviewData,View} from '../types';
import {Loading,ErrorPanel,Badge,currency,date} from './ui';
export function Overview({navigate}:{navigate:(v:View)=>void}){const [revision,setRevision]=useState(0);const {data,loading,error}=useResource<OverviewData>('/overview',revision);if(loading)return <Loading/>;if(error||!data)return <ErrorPanel message={error||'No data'} retry={()=>setRevision(v=>v+1)}/>;const max=Math.max(...data.trend.map(t=>t.totalCents),1);const points=data.trend.map((t,i)=>`${i*600/(data.trend.length-1)},${150-t.totalCents/max*125}`).join(' ');return <><div className="section-header"><div><span className="eyebrow">THE BIG PICTURE</span><h1>Operations overview<span className="heading-dot">.</span></h1><p>A clear view of your customers, orders and business activity.</p></div><button className="button secondary" onClick={()=>navigate('orders')}>View orders<ArrowUpRight size={17}/></button></div><div className="stats-grid">{[{label:'Completed revenue',value:currency(data.revenueCents),detail:'EUR · completed orders',icon:Wallet},{label:'Total customers',value:data.customers,detail:'In your workspace',icon:Users},{label:'Total orders',value:data.orders,detail:'Across all statuses',icon:ShoppingBag},{label:'Open orders',value:data.openOrders,detail:'Pending + processing',icon:Clock3}].map((s,i)=><div className={'stat-card stat-'+i} key={s.label}><div className="stat-top"><span>{s.label}</span><s.icon size={19}/></div><strong>{s.value}</strong><small>{s.detail}</small></div>)}</div><div className="overview-charts"><div className="panel chart-panel"><div className="panel-title"><div><h2>Revenue activity</h2><p>Completed orders · last 28 days</p></div><span className="small-chip">EUR</span></div><div className="chart"><div className="chart-grid"><span>{currency(max)}</span><span>{currency(max/2)}</span><span>€0</span></div><svg role="img" aria-label="Daily revenue from completed orders over the past 28 days" viewBox="0 0 600 165" preserveAspectRatio="none"><defs><linearGradient id="chart-fill" x1="0" y1="0" x2="0" y2="1"><stop stopColor="#788ef5" stopOpacity=".25"/><stop offset="1" stopColor="#788ef5" stopOpacity="0"/></linearGradient></defs><path d={`M0,165 L${points.replaceAll(' ',' L')} L600,165 Z`} fill="url(#chart-fill)"/><polyline points={points} fill="none" stroke="#6578d9" strokeWidth="2.5" vectorEffect="non-scaling-stroke"/></svg></div><div className="chart-labels"><span>{data.trend[0]?.label}</span><span>{data.trend[13]?.label}</span><span>{data.trend[27]?.label}</span></div></div><div className="panel pipeline-panel"><div className="panel-title"><div><h2>Order pipeline</h2><p>Every order, accounted for</p></div><Activity size={20}/></div><div className="pipeline-total"><strong>{data.orders}</strong><span>total orders</span></div><div className="segmented-bar">{data.statuses.map(s=><span key={s.status} className={s.status} style={{flex:s.count||.001}} title={`${s.status}: ${s.count}`}/>)}</div><div className="pipeline-list">{['completed','processing','pending','cancelled'].map(status=><div key={status}><span><i className={status}/>{status[0].toUpperCase()+status.slice(1)}</span><strong>{data.statuses.find(s=>s.status===status)?.count||0}</strong></div>)}</div></div></div><div className="panel"><div className="panel-title"><div><h2>Recent orders</h2><p>The latest activity in your workspace</p></div><button className="text-button" onClick={()=>navigate('orders')}>All orders<ArrowRight size={16}/></button></div><div className="table-scroll"><table><thead><tr><th>Order</th><th>Customer</th><th>Amount</th><th>Status</th><th>Created</th></tr></thead><tbody>{data.recentOrders.map(o=><tr key={o.id}><td><strong>{o.reference}</strong><small>{o.description}</small></td><td>{o.customerName}</td><td className="money">{currency(o.amountCents)}</td><td><Badge value={o.status}/></td><td className="muted">{date(o.createdAt)}</td></tr>)}</tbody></table></div></div><div className="bottom-note"><span><span className="green-dot"/>Live PostgreSQL data</span><span>Isolated demo workspace · fictional records</span></div></>;}
