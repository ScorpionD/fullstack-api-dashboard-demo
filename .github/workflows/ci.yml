name: Tests and production build
on:
  push:
    branches-ignore: [debugging/reproductions]
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env: {POSTGRES_USER: dashboard, POSTGRES_PASSWORD: ci-local-only, POSTGRES_DB: dashboard_test}
        ports: ["5432:5432"]
        options: --health-cmd "pg_isready -U dashboard -d dashboard_test" --health-interval 5s --health-timeout 3s --health-retries 10
    env:
      TEST_DATABASE_URL: postgres://dashboard:ci-local-only@localhost:5432/dashboard_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 22, cache: npm}
      - run: npm ci
      - run: npm test
      - run: npm run build
