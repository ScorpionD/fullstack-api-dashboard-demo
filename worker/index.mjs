export default {async fetch(request,env){const url=new URL(request.url);const reply=(status,code,message)=>Response.json({error:{code,message}},{status,headers:{'Cache-Control':'no-store'}});if(!url.pathname.startsWith('/api/'))return reply(404,'NOT_FOUND','Not found.');if(url.origin!==env.PUBLIC_ORIGIN)return reply(403,'ORIGIN_DENIED','This API is only available on the production demo.');if(!['GET','HEAD','POST','PUT','DELETE'].includes(request.method))return reply(405,'METHOD_NOT_ALLOWED','Method not allowed.');if(!['GET','HEAD'].includes(request.method)&&request.headers.get('Origin')!==env.PUBLIC_ORIGIN)return reply(403,'ORIGIN_DENIED','This origin is not allowed.');const ip=request.headers.get('CF-Connecting-IP')||'unknown';if(env.EDGE_RATE&&!(await env.EDGE_RATE.limit({key:ip})).success)return reply(429,'RATE_LIMITED','Too many requests. Please try again in a minute.');let body;if(!['GET','HEAD'].includes(request.method)){if(Number(request.headers.get('content-length')||0)>16384)return reply(413,'REQUEST_TOO_LARGE','The request is too large.');const reader=request.body?.getReader();let chunks=[],size=0;if(reader){while(true){const result=await reader.read();if(result.done)break;size+=result.value.length;if(size>16384){await reader.cancel();return reply(413,'REQUEST_TOO_LARGE','The request is too large.');}chunks.push(result.value);}}body=new Uint8Array(size);let offset=0;for(const chunk of chunks){body.set(chunk,offset);offset+=chunk.length;}}
 const headers=new Headers();for(const name of ['content-type','cookie','x-csrf-token','origin']){const value=request.headers.get(name);if(value)headers.set(name,value);}headers.set('X-Origin-Secret',env.ORIGIN_SECRET);headers.set('X-Forwarded-For',ip);
 try{const response=await env.PRIVATE_API.fetch(new Request('http://atlas-api'+url.pathname+url.search,{method:request.method,headers,body,signal:AbortSignal.timeout(15000)}));const out=new Response(response.body,response);out.headers.set('Cache-Control','no-store');return out;}catch{return reply(503,'SERVICE_UNAVAILABLE','The service is temporarily unavailable. Please retry.');}
}};
